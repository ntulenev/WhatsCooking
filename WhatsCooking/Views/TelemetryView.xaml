<UserControl x:Class="WhatsCooking.Views.TelemetryView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource SubtleBorderBrush}"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Margin="10,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="18" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Enabled" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                <TextBlock Grid.Column="1" Text="{Binding TelemetryDashboard.IsTelemetryEnabled}" FontWeight="SemiBold" />
                <TextBlock Grid.Column="3" Text="Requests" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                <TextBlock Grid.Column="4" Text="{Binding TelemetryDashboard.TelemetryRequestsCount}" FontWeight="SemiBold" />
                <TextBlock Grid.Column="6" Text="Endpoints" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                <TextBlock Grid.Column="7" Text="{Binding TelemetryDashboard.TelemetryEndpointsCount}" FontWeight="SemiBold" />
                <TextBlock Grid.Column="9" Text="Filter" Style="{StaticResource MutedTextStyle}" Margin="0,0,6,0" />
                <TextBox Grid.Column="10"
                         AutomationProperties.Name="Telemetry filter"
                         Text="{Binding TelemetryDashboard.TelemetryFilter, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>

            <DataGrid Grid.Row="1"
                      AutomationProperties.Name="Bitbucket API telemetry"
                      ItemsSource="{Binding TelemetryDashboard.TelemetryView}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding Number}" SortMemberPath="Number" Width="70" />
                    <DataGridTextColumn Header="Bitbucket API" Binding="{Binding ApiName}" SortMemberPath="ApiName" Width="*" />
                    <DataGridTextColumn Header="Requests" Binding="{Binding RequestCount}" SortMemberPath="RequestCount" Width="130" />
                    <DataGridTextColumn Header="Share" Binding="{Binding ShareText}" SortMemberPath="Share" Width="110" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
